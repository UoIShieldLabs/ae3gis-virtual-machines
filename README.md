# ae3gis-virtual-machines
This repo contains the logic to deploy VMs running a GNS3 server with AE3GIS instances and duplicate them for students to have isolated GNS3 servers with the same configurations.
